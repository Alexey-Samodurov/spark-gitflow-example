apiVersion: v2
name: data-pipeline
description: Spark data pipeline job — ETL example using PySpark
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: spark-job
    version: "0.1.0"
    repository: "file://../../charts/spark-job"
    alias: sparkjob
