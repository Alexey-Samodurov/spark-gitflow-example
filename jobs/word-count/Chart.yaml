apiVersion: v2
name: word-count
description: Spark Word Count job — example of a batch job using spark-job base chart
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: spark-job
    version: "0.1.0"
    repository: "file://../../charts/spark-job"
    alias: sparkjob
